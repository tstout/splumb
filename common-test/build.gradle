dependencies {
    compile(
            libraries.db_core,
            libraries.runtime_core,
            'junit:junit:4.10'
    )
    testCompile(
            libraries.test_core
    )


    jar {
        baseName = 'splumb-test'
    }
}